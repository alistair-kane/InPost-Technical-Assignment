# Google Maps Platform: enable Places API (Nearby Search). For --strategy geocode also enable
# Geocoding API and Place Details.
GOOGLE_MAPS_API_KEY=

# Default matches docker-compose MongoDB without auth on localhost.
MONGODB_URI=mongodb://localhost:27017/inpost_assignment
