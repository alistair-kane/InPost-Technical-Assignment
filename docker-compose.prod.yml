# Production-oriented overrides: do not publish MongoDB to the host.
# Set CORS_ORIGINS to your HTTPS Next.js origin before deploying.
#
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

# services:
#   mongo:
#     ports: []

#   api:
#     environment:
#       CORS_ORIGINS: ${CORS_ORIGINS}
