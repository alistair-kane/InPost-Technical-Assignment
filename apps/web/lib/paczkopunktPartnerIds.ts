/**
 * InPost `partner_id` values that share the Paczkopunkt marker and one
 * "Location type" filter chip in the map UI.
 */
export const PACZKOPUNKT_PARTNER_IDS: readonly number[] = [30, 33, 121];

/** Canonical id in filter UI / `selectedPartners` when any paczkopunkt type is in data. */
export const PACZKOPUNKT_UI_PARTNER_ID = 30;
